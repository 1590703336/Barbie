# 订阅追踪器 - Frontend

React + Vite + Tailwind CSS 前端应用

## 🚀 快速开始

### 安装依赖
```bash
npm install
```

### 配置环境变量
创建 `.env.local` 文件：
```env
VITE_BACKEND_API_URL=http://localhost:5000/api/v1
VITE_ML_API_URL=http://localhost:8000/api/v1
```

### 启动开发服务器
```bash
npm run dev
```

应用将在 `http://localhost:3000` 启动

### 构建生产版本
```bash
npm run build
```

## 🛠 技术栈

- React 18
- Vite
- React Router v6
- Tailwind CSS
- Zustand (状态管理)
- React Query (数据获取)
- Axios
- Recharts (图表)

## 📁 项目结构

```
src/
├── components/      # React 组件
├── pages/          # 页面组件
├── services/       # API 服务
├── store/          # 状态管理
├── hooks/          # 自定义 Hooks
├── utils/          # 工具函数
└── styles/         # 样式文件
```

